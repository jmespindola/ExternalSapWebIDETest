sap.ui.define([	"sap/ui/core/mvc/Controller",	"sap/m/MessageToast",	"sap/ui/core/routing/History"], function(Controller,MessageToast,History) {	"use strict";	var oRouter;			return Controller.extend("capu.controller.Login", {					onInit: function() {					oRouter = sap.ui.core.UIComponent.getRouterFor(this);							},		onLogin: function()	{					                                               var oView = this.getView();                 var loginInfo = new SAPB1.LoginInfo();                                  loginInfo.UserName = oView.byId("txtSapUser").getValue();                 loginInfo.Password = oView.byId("txtSapPass").getValue();                 loginInfo.CompanyDB = oView.byId("txtSapDB").getValue();                loginInfo.onActionSuccess = function(data) {                    if (data.error) {                        MessageToast.show(data.error);                        return;                    }                    else {                                           	var sessionID = "", routeID = "";                    	for (var i in data.cookies){                            if (data.cookies[i].name === "B1SESSION"){                                                            sessionID = data.cookies[i].value;                                document.cookie = data.cookies[i].value;                            }                            else if (data.cookies[i].name === "ROUTEID") {                                                            routeID = data.cookies[i].value;                            }                        }                         MessageToast.show("Bienvenido su Session es :"  + sessionID + routeID );                      oRouter.navTo("view1",true);                    }                };                        loginInfo.onActionError = function(response) {                    var resp = JSON.parse(response.responseText);                    if (resp && resp.body && resp.body.error && resp.body.error.message && resp.body.error.message.value)                    {                        MessageToast.show("Status: " + response.status + "\nB1 Msg: " + resp.body.error.message.value);                    }                    else if (resp && resp.body)                    {                        MessageToast.show("Status: " + response.status + "\nBody: " + resp.body);                    }                    else                    {                        MessageToast.show("Status: " + response.status + "\nText: " + resp);                      }                };                                             SAPB1.Session.Login(loginInfo); }		/**		 * Called when a controller is instantiated and its View controls (if available) are already created.		 * Can be used to modify the View before it is displayed, to bind event handlers and do other one-time initialization.		 * @memberOf capu.view.Login		 */				/**		 * Similar to onAfterRendering, but this hook is invoked before the controller's View is re-rendered		 * (NOT before the first rendering! onInit() is used for that one!).		 * @memberOf capu.view.Login		 */		/**		 * Called when the View has been rendered (so its HTML is part of the document). Post-rendering manipulations of the HTML could be done here.		 * This hook is the same one that SAPUI5 controls get after being rendered.		 * @memberOf capu.view.Login		 */		//	onAfterRendering: function() {		//		//	},		/**		 * Called when the Controller is destroyed. Use this one to free resources and finalize activities.		 * @memberOf capu.view.Login		 */		//	onExit: function() {		//		//	}	});});